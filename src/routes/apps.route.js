// POST create app fo site (site must be there)
// GET get app for a site (site must be there) user must be logged in and have access to the site
// DELETE delete app for a site (site must be there) user must be logged in and have access to the site
// PUT update app for a site (site must be there) user must be logged in and have access to the site
